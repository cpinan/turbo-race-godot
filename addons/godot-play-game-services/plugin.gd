@tool
extends EditorPlugin
# Stub plugin script — the actual Android .aar and GDNative bindings are
# installed separately per docs/LEADERBOARD_SETUP.md.
# This file lets the addon be registered in project.godot without errors
# on non-Android platforms.

func _enable_plugin() -> void:
	pass

func _disable_plugin() -> void:
	pass
